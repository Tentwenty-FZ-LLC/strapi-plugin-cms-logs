'use strict';

const strapiLogs     = require('./strapi-logs');
const strapiSettings = require('./strapi-settings');

module.exports = {
  'strapi-logs':     strapiLogs,
  'strapi-settings': strapiSettings,
};
